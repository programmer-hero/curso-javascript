/**
 * Crie uma função que recebe dois parâmetros, base e expoente,
 * e retorne a base elevada ao expoente.
 */
const expoente = (base, exp) => {
  const resultado = base ** exp;
  return resultado;
};

module.exports = { expoente };
