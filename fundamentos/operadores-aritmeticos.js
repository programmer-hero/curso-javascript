const [a, b, c, d] = [3, 6, 9, 12]

//operadora binários

//sintaxe in-fix
let soma = a + b + c + d

console.log(soma)

//sintaxe post-fix
soma++

//sintaxe pre-fix
--soma

const subtracao = d - b
const multiplicacao = a * b
const divisao = d / a
const modulo = a % 2 //resultado da divisão

console.log(soma, subtracao, multiplicacao, divisao, modulo)